# Incident — production POST /chat still 500 (baseline failed, LEO-046 not attempted)

**Detected:** 2026-09-17T05:09Z, pre-work baseline smoke (check 3: POST /chat → 500).
**Run:** orchestrator runId 2026-09-17T05:07:35.975Z, item LEO-046 attempt 1, model claude-opus-5.

Same signature as the Anthropic credit-balance outage first detected 2026-08-20T01:04Z
([incident-20260820T0107Z.md](incident-20260820T0107Z.md) onward; last error body captured in
[incident-20260908T0838Z.md](incident-20260908T0838Z.md)) — **day 29** if it is the same cause.

## Probes

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` c795ee5 **equals** origin/main (container booted 05:09:35Z) |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke.leo-ai.chat, `Origin: https://dosiedough.com`) | **FAIL** — 500 `{"error":"Something went wrong. Please try again."}`, three probes 05:09Z–05:10Z, each 0.4–0.6 s; `access-control-allow-origin: *` present |
| — `GET /chat/entity-name?domain=smoke.leo-ai.chat` | 200 `Leo Nightly Smoke Shop` — Mongo path healthy, so the 500 is downstream of the entity lookup |
| 4. `GET /api/admin/search` | **NOT RUN** — no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, 30 865 B, byte length identical to committed `widget/chatbot.js`, `node --check` clean |
| defer-if `GET /scrape/active` | **NOT EVALUABLE** — 401 without the admin key |

The ~0.5 s fail-fast is consistent with an immediate 4xx from the Anthropic API rather than a
timeout. The error body could NOT be confirmed this run: `GET /api/admin/logs` needs the same
admin key this host does not have.

## Action

Per the repo runbook's default-deny baseline rule: zero code work on LEO-046, nothing merged,
nothing deployed. LEO-046 recorded blocked with `baseline-failed`. No SMS/email sent, no scrape
run, no production data touched (the three smoke chats used fresh session tokens on the smoke
entity).

## Needs a human

1. Confirm the cause and top up Anthropic credits at console.anthropic.com Plans & Billing if
   that is still it — nothing in-repo can clear this.
2. This host still has no superadmin API key for smoke check 4, the `/scrape/active` defer
   condition, or reading `/api/admin/logs`.
3. Earlier runs reported that `git push origin HEAD:main` (the runbook's auto-revert) is denied on
   this host; that was not re-tested this run because nothing was merged.
