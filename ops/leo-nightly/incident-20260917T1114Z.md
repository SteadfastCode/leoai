# Incident — production POST /chat still 500 (baseline failed, LEO-049 not attempted)

**Detected:** 2026-09-17T11:14:05Z, pre-work baseline smoke (check 3: POST /chat → 500).
**Run:** orchestrator runId 2026-09-17T11:08:35.959Z, item LEO-049 attempt 1, model claude-opus-5.

Continuation of `incident-20260917T1109Z.md` (LEO-048, branch `routine/item/LEO-048`), which
itself continues the Anthropic credit-balance outage first detected 2026-08-20T01:04Z — **day 29**.

## Probes

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` 33b0f26 (booted 11:13:00Z; a deploy for the claim commit c091416 was still `DEPLOYING`) |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke.leo-ai.chat, `Origin: https://dosiedough.com`, fresh session tokens) | **FAIL** — 500 `{"error":"Something went wrong. Please try again."}`, probes 11:14:05Z (0.54 s) and 11:14:42Z (0.40 s); `access-control-allow-origin: *` present |
| 4. `GET /api/admin/search` | **NOT RUN** — no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, 30 865 B, byte-identical (`cmp`) to committed `widget/chatbot.js`, `node --check` clean |
| defer-if `GET /scrape/active` | **NOT EVALUABLE** — needs the admin key |

## Cause (confirmed)

Railway `deploymentLogs` (deployment `1240d523-a6c3-4dc4-86dc-3f9e418c818c`, the container serving
the probes) show one error per probe, at 11:14:10Z and 11:14:50Z:

```
[ERROR] [chat] 400 invalid_request_error: "Your credit balance is too low to access the Anthropic API.
Please go to Plans & Billing to upgrade or purchase credits."
    at async chat (/app/backend/src/services/claude.js:207:20)
    at async /app/backend/src/routes/chat.js:105:75
```

request_ids `req_011Cf8uxnzT7cEvjxkvxhNct`, `req_011Cf8v1UkRcB5j8ss97X2Lm`. Account billing state,
not a code regression.

## Action

Per the repo runbook's default-deny baseline rule: zero code work on LEO-049 (the dependency
refresh), no lockfile change, no PR, nothing merged, nothing deployed. A dependency bump deployed
onto a red baseline would fail post-deploy smoke check 3 regardless of its own correctness and be
auto-reverted. No SMS/email sent, no scrape run.

## Needs a human

1. Top up Anthropic credits (console.anthropic.com → Plans & Billing) for the key production uses.
2. Once the baseline is green: merge the parked PRs #38/#39/#40, then release LEO-047, LEO-048 and
   LEO-049.
3. Put a superadmin API key on this host so smoke check 4 and `/scrape/active` can run.

## Re-confirmation — LEO-049 attempt 2 (2026-09-17T11:16Z)

Same run (`2026-09-17T11:08:35.959Z`), attempt 2, re-ran the baseline before any code work:

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` c091416 (booted 11:14:53Z, deployment `48e3bc80`) |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke.leo-ai.chat, `Origin: https://dosiedough.com`, fresh session tokens) | **FAIL** — 500, probes 11:16:23Z (0.50 s) and 11:16:35Z (0.39 s); `access-control-allow-origin: *` present |
| 4. `GET /api/admin/search` | **NOT RUN** — still no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, 30 865 B, byte-identical to committed `widget/chatbot.js`, `node --check` clean |

Railway `deploymentLogs` for `48e3bc80-13a4-4218-8e9e-b43758fe497b`: the same Anthropic 400
"Your credit balance is too low" at `claude.js:207`, request_ids `req_011Cf8v8yxHqGaUftvFZtmNK` and
`req_011Cf8v9mjc1NtTGT9fLhLUS`. Again zero code work: no `yarn upgrade`, no lockfile change, no PR.
