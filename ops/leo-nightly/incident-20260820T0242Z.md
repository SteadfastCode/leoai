# Incident — 2026-08-20T02:42Z — baseline failed: Anthropic credit outage still ongoing

**Continuation of [`incident-20260820T0107Z.md`](incident-20260820T0107Z.md).** Not a new failure
mode — fresh evidence that the same external outage is still in effect ~100 minutes later.

## What happened

Run `2026-08-20T02:36:40.623Z` (fire 22:36 EDT) reached STEP 9 and ran the production baseline
smoke before claiming any work. Result:

| Check | Result | Detail |
|---|---|---|
| 1. `/health` | PASS | 200, `mongo: 1`, `commit` = `afc508c` = origin/main |
| 2. Dashboard | PASS | 200, hashed asset `/assets/index-DVceCfV_.js` → 200 |
| 3. `POST /chat` | **FAIL** | 500, generic error body |
| 4. `/api/admin/search` | PASS | 4 hits, top score 0.7912 |
| 5. `/demo/chatbot.js` | PASS | 200, byte ratio 0.975, `node --check` clean |

## Evidence (not inference)

Production console log (via `GET /api/admin/logs`) at the moment of the failed check:

```
2026-08-20T02:41:41.535Z error [ERROR] [chat] 400 {"type":"error","error":{"type":
"invalid_request_error","message":"Your credit balance is too low to access the
Anthropic API. Please go to Plans & Billing to upgrade or purchase credits."}...
```

The backend, database, RAG retrieval, dashboard, and widget are all healthy. Only the
Claude API call inside `/chat` fails, rejected upstream by Anthropic for exhausted credits.

## Action taken

Per STEP 9 (baseline fails → zero work): no item claimed, nothing merged, nothing deployed.
Run exited after writing this incident and the heartbeat.

## Resolution

Top up Anthropic API credits (Anthropic Console → Plans & Billing). No code change needed;
no revert applicable — origin/main (`afc508c`) is deployed and healthy apart from the
upstream rejection. The next fire's baseline will pass on its own once credits exist.

Subsequent fires tonight will hit the same baseline failure until credits are topped up;
notification suppression is recorded in the local status file
(`C:/Users/danie/.claude/leo-nightly/last-run.json`, `baselineFailNotifiedAtUtc`) so this
should be reported once, not hourly.

**Update 2026-08-20T07:41Z (runId 2026-08-20T07:36:44.674Z):** still ongoing. Baseline
check 3 (POST /chat) → 500; production log at 07:40:44Z shows the same
`credit balance is too low` 400 from Anthropic. Checks 1, 2, 4, 5 all pass
(health commit 255b69b + mongo 1, dashboard + asset 200, RAG top 0.7913,
widget ratio 0.975 + syntax OK). Zero work done; notification suppressed
(already sent 01:07Z and 02:43Z).
