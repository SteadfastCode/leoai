# Incident — production POST /chat still 500 (baseline failed, LEO-047 not attempted)

**Detected:** 2026-09-17T05:16Z, pre-work baseline smoke (check 3: POST /chat → 500).
**Run:** orchestrator runId 2026-09-17T05:07:35.975Z, item LEO-047 attempt 1, model claude-opus-5.

Continuation of [incident-20260917T0510Z.md](incident-20260917T0510Z.md) (same run, LEO-046; that
file lives on branch `routine/item/LEO-046`). Same signature as the Anthropic credit-balance outage
first detected 2026-08-20T01:04Z — **day 29** if it is the same cause.

## Probes

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` 25aa096 (container booted 05:12:48Z; origin/main has since moved to 662fcf8, ops-only claim commit) |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke.leo-ai.chat, `Origin: https://dosiedough.com`) | **FAIL** — 500 `{"error":"Something went wrong. Please try again."}`, two probes 05:16Z and 05:17:24Z, 0.39 s / 0.40 s; `access-control-allow-origin: *` present |
| — `GET /chat/entity-name?domain=smoke.leo-ai.chat` | 200 `Leo Nightly Smoke Shop` — Mongo path healthy |
| 4. `GET /api/admin/search` | **NOT RUN** — no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, 30 865 B, byte-identical length to committed `widget/chatbot.js`, `node --check` clean |
| defer-if `GET /scrape/active` | **NOT EVALUABLE** — 401 without the admin key |

## Action

Per the repo runbook's default-deny baseline rule: zero code work on LEO-047, nothing merged,
nothing deployed. LEO-047 recorded blocked with `baseline-failed`. No SMS/email sent, no scrape
run, no production data touched (both smoke chats used fresh session tokens on the smoke entity).

Build-and-park (the LEO-042/043/046 precedent) was deliberately NOT used here: LEO-047 edits the
same hunks as the already-parked PR #38 (LEO-042) and PR #39 (LEO-043) — the `Entity.js` schema,
the `allowed` array in `routes/dashboard.js`, and a new card in `Settings.vue` — so a third parked
PR would be written against a base that is about to change under it and would need rework after
those two merge anyway.

## Needs a human

1. Confirm the cause and top up Anthropic credits at console.anthropic.com if that is still it.
2. Merge the parked PRs #38, #39, #40 once the baseline is green, then release LEO-047 so it is
   built on top of them.
3. This host still has no superadmin API key (smoke check 4, `/scrape/active`, `/api/admin/logs`).
