# Incident — Anthropic credit outage STILL ongoing (night of 2026-08-28, baseline failed)

**Detected:** 2026-08-29T02:40Z, STEP 9 baseline smoke (check 3: POST /chat → 500).
**Run:** leo-nightly runId 2026-08-29T02:37:14Z, nightId 2026-08-28 (first working-eligible fire of the night — backpressure released this evening by the ack tag moving to 9e011d8).

Same external outage as [incident-20260820T0107Z.md](incident-20260820T0107Z.md),
[incident-20260821T0040Z.md](incident-20260821T0040Z.md), and
[incident-20260822T0040Z.md](incident-20260822T0040Z.md). This time the error body was captured
from the production log buffer (`GET /api/admin/logs`):

> `[chat] 400 {"type":"error","error":{"type":"invalid_request_error","message":"Your credit
> balance is too low to access the Anthropic API. Please go to Plans & Billing to upgrade or
> purchase credits."}}` — request ids `req_011CeWGRcvQwKk3PYw92KBV2`, `req_011CeWGQ5iuitdi5inZ4bg77`
> (both from this run's smoke/diagnostic requests, thrown at `claude.js:207` via `chat.js:105`).

First detected 2026-08-20T01:04Z — **~9 days ago**. Whether credits were topped up and
re-exhausted in between is not determinable from here (backpressure halted all fires 8-21→8-27,
so no chat smoke ran in the gap); what is certain is the balance is exhausted right now.

Other baseline results:

- health: 200, mongo 1 — but `commit` is `fcfbf65`, **2 heartbeat commits behind** origin/main
  `9e011d8`. Cause (from Railway API): deployment `df34fbff` for 9e011d8 has been stuck
  `DEPLOYING` since 00:38Z — image pushed 01:56Z, healthcheck succeeded 02:31Z, status never
  flipped as of 02:43Z. The 1297fb7 deploy shows `REMOVED` (superseded). The lag is
  Railway-side and the diff between served and tip is heartbeat-only (`ops/leo-nightly/
  last-run.json`), so no app-code divergence in production. Watch that the stuck deploy
  eventually resolves; a build system wedged mid-deploy would also block the next real merge.
- dashboard: PASS (hashed asset `index-DVceCfV_.js` 200)
- admin search: PASS (4 hits, top 0.7912 — the usual margin)
- widget: PASS (30753 bytes, `node --check` ok)

Only the Claude call path fails. Zero work per STEP 9 default-deny — any post-deploy smoke
would fail the same check 3, so nothing can ship until the credit balance is topped up
(Console → Plans & Billing). The queue has 3 eligible items waiting (LEO-040, LEO-042, LEO-043)
and backpressure is now clear (unreviewed = 0), so the routine resumes work automatically on
the first fire after chat smokes green.
