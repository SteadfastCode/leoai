# Incident — 2026-08-07T09:44:52Z — production baseline smoke failed

**Run:** leo-nightly, runId `2026-08-07T09:36:59.121Z` (fire at 05:37 local)
**Decision:** `baseline-failed` — zero item work performed. No item claimed, `LEO-001.attempts` is still 0.
**Production sha at time of check:** `2d926d4bccfd97dfb2d0113c04df5946b10da4c4` (current `origin/main` tip).

## Result

| # | Check | Result |
|---|---|---|
| 1 | `GET /health` | **PASS** — `commit` matches `origin/main`, `mongo: 1` |
| 2 | `GET https://leo-ai.app` + hashed asset | **PASS** — 200; `/assets/index-CY4f5rDv.js` 200 |
| 3 | `POST /chat` (Origin `https://dosiedough.com`) | **PASS** — 200, `access-control-allow-origin: *`, 185-char reply |
| 4 | `GET /api/admin/search` scored hit | **PASS** — 4 hits, topScore `0.7912` (bar: >= 0.75) |
| 5 | `GET /demo/chatbot.js` | **FAIL — HTTP 404** |

## The failure

`GET https://api.leo-ai.chat/demo/chatbot.js` returns **404**. Probed siblings, all 404:

```
/demo/chatbot.js  -> 404
/demo/            -> 404
/demo/index.html  -> 404
/widget/chatbot.js-> 404
/chatbot.js       -> 404
```

What is established:

- `backend/index.js:73` mounts `app.use('/demo', express.static(path.join(__dirname, '../widget'), ...))`.
- `widget/chatbot.js` **is** committed, at the repo root (`widget/` holds `chatbot.js`, `chatbot.css`, `test.html`).
- So the mount resolves to `<repo-root>/widget` in a full checkout, and the file exists there.
- Nothing was deployed by this run. This is a **pre-existing production condition**, not a regression
  introduced tonight.

What is **not** established: why the path is missing in the deployed container. The most likely
explanation is that Railway's build/deploy root is `backend/`, so `../widget` resolves outside the
image and the directory simply is not present — but the Railway service configuration lives outside
this repo and was **not** inspected, so that is an unverified hypothesis, not a finding. It could
equally be a `.dockerignore`/nixpacks file-inclusion effect. Do not act on the guess without
checking the actual deploy config.

## Why this halted the run

STEP 9 is default-deny: the full smoke runs against production *before* any code is touched, and a
failing baseline means stop. That rule exists precisely for this case — had the run proceeded, the
post-deploy smoke would have failed on check 5 for a reason having nothing to do with the change,
the routine would have concluded its own merge broke production, and it would have reverted healthy
code while the smoke still failed.

## What Daniel needs to decide

The routine is **hard-blocked** until this resolves — every future fire will fail the same baseline
and do nothing. Options:

1. **Fix the deploy** so `widget/` ships with the backend image (correct if `/demo/chatbot.js` is
   meant to be publicly served — it is the widget's own embed target and `test.html` references it).
2. **Change check 5** in the routine's smoke if `/demo` is dev-only and was never meant to be live in
   production. The routine may not edit its own safety net, so this is Daniel's call to make.

Until one of those happens, leo-nightly ships nothing.

## Fixture created this run

A dedicated smoke entity did not exist, so one was created per STEP 14 (see
`ops/leo-nightly/README.md`). Checks 3 and 4 above ran against it and passed.
