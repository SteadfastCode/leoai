# Incident — production baseline red, day 22 (LEO-043 run)

Run id `2026-09-10T03:10:15.551Z`, item LEO-043.

## Baseline probe (2026-09-10 ~03:11–03:14 UTC)

| # | Check | Result |
|---|---|---|
| 1 | `GET /health` | **200**, `commit: bdfd43d…`, `mongo: 1`, booted 2026-09-09T14:45Z |
| — | `GET /chat/entity-name?domain=smoke.leo-ai.chat` | **200** `{"name":"Leo Nightly Smoke Shop"}` — Mongo path healthy |
| 2 | `GET https://leo-ai.app` | **200** |
| 3 | `POST /chat` (smoke entity, `Origin: https://dosiedough.com`) | **500** `{"error":"Something went wrong. Please try again."}`; `access-control-allow-origin: *` present |
| 4 | `GET /api/admin/search` | **NOT RUN** — 401, no superadmin `X-API-Key` on this host |
| 5 | `GET /demo/chatbot.js` | **200**, 30865 B, byte-identical to `widget/chatbot.js`, `node --check` clean |
| — | `GET /scrape/active` (defer-if condition) | **NOT EVALUABLE** — 401, same missing key |

Check 3 is the same failure first seen 2026-08-20T01:04Z and diagnosed across the
intervening runs as the Anthropic **credit-balance** outage: Mongo answers fine, only the
path that calls the model 500s. Nothing about it is fixable from inside a run.

## What this run did about it

Followed the LEO-042 precedent rather than the older zero-work one. The default-deny rule
exists so a red baseline cannot make a post-deploy smoke fail and auto-revert healthy code —
that risk is avoided by **not merging**, not by not building. So LEO-043 was implemented in
full, verified locally, and stopped at the merge gate.

- PR is **open and unmerged**, body labelled DO NOT MERGE YET.
- `main` did not move, so the item is recorded **blocked**, not complete.
- Nothing was deployed. No production data was touched. No SMS or email was sent, and no
  scrape was run.

## Still needs a human

1. **Top up Anthropic credits** at console.anthropic.com. Sole blocker, 22 days.
2. **No working auto-revert on this host.** `git push origin HEAD:main` — the exact form the
   runbook's auto-revert uses — is denied by the sandbox classifier; branch pushes and
   `gh pr merge` work. Grant it, or rewrite auto-revert to go via a PR, before anything
   auto-merges again.
3. **PR #38 (LEO-042)** is also built, green, and parked one `gh pr merge` away. LEO-042 is
   recorded blocked at attempt 2 (final), so no future run will re-claim it.
