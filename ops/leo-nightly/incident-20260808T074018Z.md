# Incident — 2026-08-08T07:40:18Z — production baseline smoke failed (2 checks)

**Run:** leo-nightly, runId `2026-08-08T07:37:18.528Z` (fire at 03:37 local)
**Decision:** `baseline-failed` — zero item work performed. No item claimed, `LEO-001.attempts` is still 0.
**`origin/main` tip:** `fdd96fc537b53f9df2af0d7f3b4fdbabe94b3a3d`
**Commit production is actually serving:** `239a3a78e16ed20a1ad72cf95963f051a6b3067a`

This is the second consecutive baseline failure. It is **not** a repeat of the previous one — check 1
now fails as well, and that is the more informative signal.

## Result

| # | Check | Result |
|---|---|---|
| 1 | `GET /health` commit == `origin/main`, `mongo == 1` | **FAIL** — `mongo: 1` OK, but `commit` is `239a3a7`, one commit behind `origin/main` (`fdd96fc`) |
| 2 | `GET https://leo-ai.app` + hashed asset | **PASS** — 200; `/assets/index-CY4f5rDv.js` 200 |
| 3 | `POST /chat` (Origin `https://dosiedough.com`) | **PASS** — 200, `access-control-allow-origin: *`, 228-char reply |
| 4 | `GET /api/admin/search` scored hit | **PASS** — 4 hits, topScore `0.79115` (bar: >= 0.75) |
| 5 | `GET /demo/chatbot.js` | **FAIL — HTTP 404** (body 154 bytes, an error page) |

## What is established

- `fdd96fc` ("Add root-level nixpacks.toml for Railway root-directory move") was committed
  **2026-08-07 23:27:40 -0400**, roughly 4 hours before this check, and is the current `origin/main` tip.
- It changes exactly one file: a new root-level `nixpacks.toml`, 24 insertions, nothing else.
- Production `/health` reports commit `239a3a7` — the commit *before* it. So `fdd96fc` is **not running
  in production**, 4 hours after it landed on `main`.
- `/demo/chatbot.js` still returns 404, unchanged from the 2026-08-07 incident. That is consistent
  with `fdd96fc` not being live: the fix it carries has never actually been applied to a running container.
- Checks 2, 3 and 4 pass, so the dashboard, the chat path (including CORS from a third-party origin)
  and the RAG retrieval layer are all healthy. topScore `0.79115` is essentially identical to
  yesterday's `0.7912` — no RAG drift.

## What is NOT established

**Why `fdd96fc` has not deployed.** I have no Railway access and did not inspect the service
configuration; the following are hypotheses, explicitly not findings:

- Railway may not have triggered a build at all. If the service's Root Directory is still `backend/`,
  Railway commonly watches only that path, and `fdd96fc` touches nothing under `backend/` — so a push
  containing only a root-level file could be ignored rather than deployed.
- Or a build was triggered and failed, in which case Railway keeps serving the previous container,
  which answers 200 on `/health` exactly as it does now.

These produce identical symptoms from outside. Distinguishing them requires the Railway build log,
which lives outside this repo.

Note also that the commit's own header comment states the config is **inert until the Railway
service's Root Directory is set to `/`** — that is a dashboard change, made by hand, outside version
control. Whether that change has been made is not observable from here.

## What Daniel needs to do

The routine remains **hard-blocked** — every fire will fail this same baseline and ship nothing.

1. Open the Railway build log for the service and confirm whether a deploy for `fdd96fc` was
   triggered, and if so whether it succeeded.
2. If the Root Directory has not yet been flipped to `/`, flip it — the committed root `nixpacks.toml`
   does nothing until then, by its own documentation.
3. Re-check `GET https://api.leo-ai.chat/health` reports `fdd96fc…` and
   `GET https://api.leo-ai.chat/demo/chatbot.js` returns 200.

Alternatively, if `/demo` is dev-only and was never meant to be served in production, check 5 belongs
out of the routine's smoke. The routine may not edit its own safety net, so that is Daniel's call.

## Fixture

Unchanged — smoke entity `smoke.leo-ai.chat`, 4 manual chunks, documented in
[`ops/leo-nightly/README.md`](README.md). No scrape was run. No SMS or email could fire (both owner
contact fields are blank by design).
