# Incident — production POST /chat still 500 (baseline failed, LEO-050 not attempted)

**Detected:** 2026-09-17T18:12:40Z, pre-work baseline smoke (check 3: POST /chat → 500).
**Run:** orchestrator runId 2026-09-17T18:11:35.963Z, item LEO-050 attempt 1, model claude-opus-5.

Continuation of `incident-20260917T1114Z.md` (LEO-049, branch `routine/item/LEO-049`), which
continues the Anthropic credit-balance outage first detected 2026-08-20T01:04Z — **day 29**.

## Probes

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` 87a0361 (booted 11:19:04Z; origin/main is the claim commit fb72c77) |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke.leo-ai.chat, `Origin: https://dosiedough.com`, fresh session tokens) | **FAIL** — 500 `{"error":"Something went wrong. Please try again."}`, probes 18:12:40Z (0.38 s) and 18:12:51Z (0.36 s); `access-control-allow-origin: *` present |
| 4. `GET /api/admin/search` | **NOT RUN** — no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, 30 865 B, same length as committed `widget/chatbot.js`, `node --check` clean |
| defer-if `GET /scrape/active` | **NOT EVALUABLE** — needs the admin key |

## Cause (not re-confirmed this run)

No Railway API token was reachable from this run (`RAILWAY_TOKEN` unset, no `railway.token` file),
so the deployment logs could not be read. The failure signature is identical to the two
log-confirmed probes earlier today (11:14Z, 11:16Z): a 500 in well under a second with the generic
error body, which is how the Anthropic 400 "Your credit balance is too low to access the Anthropic
API" surfaces through `claude.js`. Treat it as the same account-billing outage until a log read says
otherwise.

## Action

Per the repo runbook's default-deny baseline rule: zero code work on LEO-050 (alpha usage report),
no PR, nothing merged, nothing deployed. Post-deploy smoke check 3 would fail no matter what the
change did, and the auto-revert would roll back healthy code. No SMS/email sent, no scrape run.

## Needs a human

1. Top up Anthropic credits (console.anthropic.com → Plans & Billing) for the key production uses.
2. Once the baseline is green: merge the parked PRs #38/#39/#40, then release LEO-047, LEO-048,
   LEO-049 and LEO-050.
3. Put a superadmin API key and `RAILWAY_TOKEN` on this host so smoke check 4, `/scrape/active` and
   cause confirmation can run.
