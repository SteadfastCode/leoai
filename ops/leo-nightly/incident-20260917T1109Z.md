# Incident — production POST /chat still 500 (baseline failed, LEO-048 not attempted)

**Detected:** 2026-09-17T11:09:24Z, pre-work baseline smoke (check 3: POST /chat → 500).
**Run:** orchestrator runId 2026-09-17T11:08:35.959Z, item LEO-048 attempt 1, model claude-opus-5.

Continuation of `incident-20260917T0517Z.md` (LEO-047, branch `routine/item/LEO-047`). Same
signature as the Anthropic credit-balance outage first detected 2026-08-20T01:04Z — **day 29** if
it is the same cause. Cause not independently confirmed this run: no Railway token and no
superadmin API key on this host, so neither Railway logs nor `/api/admin/logs` were readable.

## Probes

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` b46a1fb (container booted 05:21:30Z; origin/main has since moved to 6ae6a52, ops-only claim commit) |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke.leo-ai.chat, `Origin: https://dosiedough.com`) | **FAIL** — 500 `{"error":"Something went wrong. Please try again."}`, two probes 11:09:24Z (1.04 s) and 11:10:24Z (0.36 s); `access-control-allow-origin: *` present |
| — `GET /chat/entity-name?domain=smoke.leo-ai.chat` | 200 `Leo Nightly Smoke Shop` — Mongo path healthy |
| 4. `GET /api/admin/search` | **NOT RUN** — no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, 30 865 B, byte-identical (`cmp`) to committed `widget/chatbot.js`, `node --check` clean |
| defer-if `GET /scrape/active` | **NOT EVALUABLE** — 401 without the admin key |

## Action

Per the repo runbook's default-deny baseline rule ("before touching code … if it fails, block the
item with `baseline-failed` … and exit"): zero code work on LEO-048, no PR, nothing merged, nothing
deployed. No SMS/email sent, no scrape run; both smoke chats used fresh session tokens on the smoke
entity.

Build-and-park (the LEO-042/043/046 precedent) was not used. LEO-048 would not conflict with the
parked PRs (it is a `scraper.js` + new-test-file change), but the runbook's rule is explicit, and
three parked PRs (#38, #39, #40, oldest 2026-09-09) are already waiting on the same human action —
a fourth adds review load without shortening the path to green.

## Needs a human

1. Confirm the cause of the /chat 500 and top up Anthropic credits at console.anthropic.com if that
   is still it.
2. Merge the parked PRs #38, #39, #40 once the baseline is green, then release LEO-047 and LEO-048.
3. This host still has no superadmin API key (smoke check 4, `/scrape/active`, `/api/admin/logs`)
   and no Railway token (the memory note points at a Windows path), so the cause of the 500 cannot
   be read from here.

## Re-confirmation — attempt 2 (same run, 2026-09-17T11:11:23Z claim)

The orchestrator re-claimed LEO-048 22 s after attempt 1 blocked. Baseline re-run before any code
work; still red, so again zero code work.

| Check | Result |
|---|---|
| 1. `GET /health` | PASS — 200, `mongo` 1, `commit` 6ae6a52, booted 11:10:26Z |
| 2. `GET https://leo-ai.app` + hashed asset | PASS — 200, `/assets/index-DVceCfV_.js` 200 |
| 3. `POST /chat` (smoke entity, `Origin: https://dosiedough.com`) | **FAIL** — 500, probes 11:12:00Z (0.59 s) and 11:12:14Z (0.44 s); `access-control-allow-origin: *` present |
| 4. `GET /api/admin/search` | **NOT RUN** — still no superadmin `X-API-Key` on this host |
| 5. `GET /demo/chatbot.js` | PASS — 200, byte-identical (`cmp`) to committed file, `node --check` clean |

**Cause now confirmed** (correction to attempt 1's "no Railway token on this host": `RAILWAY_TOKEN`
is set in this host's environment and the GraphQL API accepts it). Railway `deploymentLogs` for
deployment `0dff5542-5a67-4ac2-a683-c04b4b3dcd19` (SUCCESS, 11:08:52Z) show, for both probes:

```
[ERROR] [chat] 400 invalid_request_error: "Your credit balance is too low to access the Anthropic API.
Please go to Plans & Billing to upgrade or purchase credits."
    at async chat (/app/backend/src/services/claude.js:207:20)
    at async /app/backend/src/routes/chat.js:105:75
```

request_ids `req_011Cf8uoWtpvthwtkE8SPC5g`, `req_011Cf8upZNgLDP8EGWeNdaau`. This is an account
billing state, not a code regression — no revert or code change can fix it.
