# Incident — production baseline red, day 22 (LEO-043, attempt 2)

Run id `2026-09-10T10:08:15.573Z`, item LEO-043. Second claim of this item; attempt 1
(`2026-09-10T03:10:15.551Z`) built it and parked it as PR #39 — see
`incident-20260910T0319Z.md`.

## Baseline probe (2026-09-10 ~10:09–10:11 UTC)

| # | Check | Result |
|---|---|---|
| 1 | `GET /health` | **200**, `mongo: 1`; commit `1d27b85…` at 10:09Z, then `1f9185c…` booted **10:10:02Z** (Railway redeployed the claim commit mid-probe) |
| — | `GET /chat/entity-name?domain=smoke.leo-ai.chat` | **200** `{"name":"Leo Nightly Smoke Shop"}` — Mongo path healthy |
| 2 | `GET https://leo-ai.app` + hashed `/assets/index-DVceCfV_.js` | **200** / **200** |
| 3 | `POST /chat` (smoke entity, `Origin: https://dosiedough.com`) | **500** `{"error":"Something went wrong. Please try again."}` in ~0.4 s, `access-control-allow-origin: *` present. Probed twice; the second probe (10:10:59Z) hit the container booted 57 s earlier, so this is not a stale process |
| 4 | `GET /api/admin/search` | **NOT RUN** — no superadmin `X-API-Key` on this host |
| 5 | `GET /demo/chatbot.js` | **200**, 30865 B, byte-identical to `widget/chatbot.js`, `node --check` clean |
| — | `GET /scrape/active` (defer-if condition) | **NOT EVALUABLE** — 401, same missing key |

Same signature as the Anthropic **credit-balance** outage first seen 2026-08-20T01:04Z: Mongo
answers, the model-calling path fails fast (~0.4 s is an immediate API rejection, not a 529 or
a timeout). Nothing in-repo can fix it.

## What this run did

- **No code work.** LEO-043 was already built, verified and parked by attempt 1; rebuilding it
  would have produced the same PR.
- Brought PR #39's branch current with `main` (merge of `origin/main`, ops-only: `ledger.jsonl`,
  `state.json` — no restricted file changed in the merge) and added this write-up, so the PR
  stays one clean `gh pr merge 39 --merge` away.
- **Did not merge, did not deploy.** `main` did not move for this item. No production data
  touched, no SMS or email sent, no scrape run.

## Still needs a human

1. **Top up Anthropic credits** at console.anthropic.com → Plans & Billing. Sole blocker, day 22.
2. **PR #39 (LEO-043)** and **PR #38 (LEO-042)** are built, CI-green and parked. Once the baseline
   smoke passes, merge with `--merge` (never squash) and run the post-deploy smoke from
   `ops/leo-nightly/RUNBOOK.md`.
3. **Auto-revert on this host** was reported unavailable by earlier runs (`git push origin
   HEAD:main` denied by the sandbox classifier). Confirm it works — or move auto-revert to a
   revert PR — before any run auto-merges again.
